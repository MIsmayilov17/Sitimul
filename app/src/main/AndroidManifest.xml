<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sitimulacademi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launc_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SitimulAcademi">
        <activity
            android:name=".Dinleme"
            android:exported="true" />
        <activity
            android:name=".Sellect_random"
            android:exported="true" />
        <activity
            android:name=".guiz"
            android:exported="true" />
        <activity
            android:name=".listning"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>